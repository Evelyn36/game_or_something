using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class swordanimationfinish : MonoBehaviour
{
    // Start is called before the first frame update


   

    public void DeleteMe()
    {
        Destroy(gameObject);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
